info:
  name: Savings Response
  type: http
  seq: 1

http:
  method: POST
  url: "{{rabbitmq}}/api/exchanges/%2F/amq.default/publish"
  body:
    type: json
    data: |-
      {
        "properties": {
          "content_type": "application/json",
          "delivery_mode": 2
        },
        "routing_key": "ecoscan.ai.results.savings",
        "payload": "{\"jobId\": \"{{savingsJobId}}\", \"userId\": \"{{userId}}\", \"data\": {\"co2Saving\": 142.75, \"carRideEquivalent\": 35}}",
        "payload_encoding": "string"
      }
  auth: inherit

runtime:
  variables:
    - name: savingsJobId
      value: a1b2c3d4-e5f6-7a8b-9c0d-1e2f3a4b5c6d
    - name: userId
      value: 00000001-0000-0000-0000-000000000001

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
