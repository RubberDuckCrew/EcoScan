info:
  name: User Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{backend}}/api/users/register"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
