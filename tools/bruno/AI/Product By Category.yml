info:
  name: Product By Category
  type: http
  seq: 1

http:
  method: GET
  url: "{{backend}}/product/by-category/Kaugummi"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
