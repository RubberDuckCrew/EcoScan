info:
  name: History Savings
  type: http
  seq: 1

http:
  method: GET
  url: "{{backend}}/history/savings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
